# sample

> **Language:** typescript
> **Generated by:** fastdoc | 24/03/2026

## Overview

A sample TypeScript module containing user management functionality and utility functions for discount calculations

---

## 📐 `User`

**Type:** `interface`

Defines the structure of a user entity with basic identification and contact information

### Signature

```
export interface User
```

### Returns

`interface` — Type definition for User objects

### Example

```typescript
const user: User = { id: 1, name: 'John Doe', email: 'john@example.com' };
```

---

## 🏛️ `UserService`

**Type:** `class`

A service class that manages user operations including retrieval and creation of user entities

### Signature

```
export class UserService
```

### Returns

`UserService` — Instance of UserService class

### Example

```typescript
const userService = new UserService();
const user = await userService.createUser('Jane Doe', 'jane@example.com');
```

> ⚠️ **Warning:** Uses Date.now() for ID generation which may not be suitable for production use due to potential collisions

---

## 🔧 `getUser`

**Type:** `function`

Retrieves a user by their unique identifier from the internal users array

### Signature

```
async getUser(id: number): Promise<User | undefined>
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `id` | `number` | The unique identifier of the user to retrieve |

### Returns

`Promise<User | undefined>` — A promise that resolves to the User object if found, otherwise undefined

### Example

```typescript
const userService = new UserService();
const user = await userService.getUser(123);
```

---

## 🔧 `createUser`

**Type:** `function`

Creates a new user with the provided name and email, automatically generating a unique ID

### Signature

```
async createUser(name: string, email: string): Promise<User>
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | The full name of the user to create |
| `email` | `string` | The email address of the user to create |

### Returns

`Promise<User>` — A promise that resolves to the newly created User object

### Example

```typescript
const userService = new UserService();
const newUser = await userService.createUser('Alice Smith', 'alice@example.com');
```

> ⚠️ **Warning:** Uses Date.now() for ID generation which may cause collisions if called rapidly

---

## 🔧 `calculateDiscount`

**Type:** `function`

Calculates the discounted price by applying a discount rate to the original price

### Signature

```
export function calculateDiscount(price: number, rate: number): number
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `price` | `number` | The original price before discount |
| `rate` | `number` | The discount rate as a decimal between 0 and 1 (e.g., 0.1 for 10% discount) |

### Returns

`number` — The final price after applying the discount

### Example

```typescript
const discountedPrice = calculateDiscount(100, 0.2); // Returns 80
```

> ⚠️ **Warning:** Throws Error if rate is not between 0 and 1

---
