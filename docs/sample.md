# sample

> **Language:** typescript
> **Generated by:** fastdoc | 13/03/2026

## Overview

A TypeScript module containing user management functionality with interfaces, services, and utility functions for handling user data and discount calculations.

---

## 📐 `User`

**Type:** `interface`

Defines the structure of a user object with essential user information

### Signature

```
interface User
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `id` | `number` | Unique identifier for the user |
| `name` | `string` | The full name of the user |
| `email` | `string` | The email address of the user |

### Returns

`User` — User interface type definition

### Example

```typescript
const user: User = { id: 1, name: 'John Doe', email: 'john@example.com' };
```

---

## 🏛️ `UserService`

**Type:** `class`

Service class for managing user operations including retrieval and creation of users

### Signature

```
class UserService
```

### Returns

`UserService` — Instance of UserService class

### Example

```typescript
const userService = new UserService();
const user = await userService.createUser('Alice', 'alice@example.com');
const foundUser = await userService.getUser(user.id);
```

> ⚠️ **Warning:** Users are stored in memory and will be lost when the service instance is destroyed

---

## 🔧 `getUser`

**Type:** `function`

Retrieves a user by their unique identifier

### Signature

```
async getUser(id: number): Promise<User | undefined>
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `id` | `number` | The unique identifier of the user to retrieve |

### Returns

`Promise<User | undefined>` — Promise that resolves to the user object if found, otherwise undefined

### Example

```typescript
const userService = new UserService();
const user = await userService.getUser(123);
```

---

## 🔧 `createUser`

**Type:** `function`

Creates a new user with the provided name and email, automatically generating a unique ID

### Signature

```
async createUser(name: string, email: string): Promise<User>
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | The full name of the user to create |
| `email` | `string` | The email address of the user to create |

### Returns

`Promise<User>` — Promise that resolves to the newly created user object

### Example

```typescript
const userService = new UserService();
const newUser = await userService.createUser('Jane Smith', 'jane@example.com');
```

> ⚠️ **Warning:** ID is generated using Date.now() which may not guarantee uniqueness in high-frequency scenarios

---

## 🔧 `calculateDiscount`

**Type:** `function`

Calculates the discounted price based on the original price and discount rate

### Signature

```
function calculateDiscount(price: number, rate: number): number
```

### Parameters

| Name | Type | Description |
|------|------|-------------|
| `price` | `number` | The original price before discount |
| `rate` | `number` | The discount rate as a decimal between 0 and 1 (e.g., 0.1 for 10% discount) |

### Returns

`number` — The final price after applying the discount

### Example

```typescript
const discountedPrice = calculateDiscount(100, 0.2); // Returns 80
```

> ⚠️ **Warning:** Throws Error if rate is not between 0 and 1 (inclusive)

---
